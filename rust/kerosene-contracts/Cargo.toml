[package]
name = "kerosene-contracts"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Canonical Kerosene cross-repository wire contracts"

[features]
default = []
production = []

[dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
